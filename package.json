{
  "name": "blackpink-fan-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run start",
    "install": "cd frontend && npm install --legacy-peer-deps"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}